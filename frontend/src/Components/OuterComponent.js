import { Typography } from "@mui/material";
import React from "react";

function OuterComponent() {
  return (
    <Typography variant="h1" sx={{ color: "mycolor" }}>
      Outer compo
    </Typography>
  );
}

export default OuterComponent;
